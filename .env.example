# Security
# Required. Must be a unique, private value — the app refuses to start with no
# value or a known placeholder. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=